# Loop9 mainsite (front website)

Static marketing site from the Claude design handoff (`AI call center dashboard 2060.zip`).

- Git branch: `mainsite`
- Railway service: `mainsite` (production canvas, separate from `loop9` app)

Local preview: `cd mainsite && node server.js` then open http://localhost:3000
